---
paths:
  - "frontend/**"
---
# Frontend rules (path-scoped)

- Canary token for this rule: `FRONTEND-RULE-2213`.
- Styling: CSS modules only, no inline `style={{}}`.
- Every component gets a `data-testid` equal to its file name in kebab-case.
